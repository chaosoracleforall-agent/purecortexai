---
title: "Python SDK"
description: "Interact with PURECORTEX from Python using httpx, websockets, and algosdk."
icon: "python"
---

# Python SDK

This guide covers the official Python SDK in `sdk/python`. It wraps the current REST API, authenticated chat-session bootstrap flow, and governance/transparency reads. Use raw `algosdk` only when you need direct on-chain access that is intentionally outside the HTTP API boundary.

## Installation

```bash
pip install ./sdk/python
```

---

## Quick Start

```python
from purecortex_sdk import PureCortexClient

with PureCortexClient(api_key="ctx_your_key") as client:
    health = client.health()
    print(health["dependencies"])

    supply = client.supply()
    print(supply["total_supply"])

    registry = client.list_agents()
    print(registry["total_agents"])

    reply = client.chat("senator", "Summarize the governance system.")
    print(reply["response"])
```

## Current Client Methods

- `health()`
- `supply()`, `treasury()`, `burns()`, `governance_transparency()`, `transparency_agents()`
- `list_agents()`, `agent_activity(agent_name)`, `chat(agent_name, message)`
- `constitution()`, `governance_overview()`, `list_proposals()`, `proposal(id)`, `onchain_proposals()`
- `create_proposal(...)`, `review_proposal(...)`, `vote(...)`
- `create_chat_session()`, `websocket_url(session_token)`, `websocket_chat(messages)`
- `bootstrap_admin_key(...)`, `create_api_key(...)`, `revoke_api_key(...)`

---

## REST Examples

### Supply, Agents, and Governance

```python
from purecortex_sdk import PureCortexClient

with PureCortexClient() as client:
    supply = client.supply()
    overview = client.governance_overview()
    agents = client.list_agents()

    print(supply["circulating"])
    print(overview["total_votes"])
    print([agent["name"] for agent in agents["agents"]])
```

### Chat with an Agent

```python
from purecortex_sdk import PureCortexClient

with PureCortexClient(api_key="ctx_your_key") as client:
    senator = client.chat("senator", "What is the current governance status?")
    curator = client.chat("curator", "How does composability scoring work?")
    print(senator["response"])
    print(curator["response"])
```

### Proposal Detail and On-Chain Governance Read

```python
from purecortex_sdk import PureCortexClient

with PureCortexClient() as client:
    proposal = client.proposal(1)
    onchain = client.onchain_proposals()
    print(proposal["title"])
    print(onchain["total"])
```

---

## WebSocket Chat

Real-time bidirectional chat with PURECORTEX AI agents.

```python
import asyncio
from purecortex_sdk import PureCortexClient


async def main() -> None:
    with PureCortexClient(api_key="ctx_your_key") as client:
        responses = await client.websocket_chat(
            [
                "What is the CORTEX token used for?",
                "How does staking work?",
            ]
        )
        for response in responses:
            print(response)


asyncio.run(main())
```

<Warning>
  WebSocket messages are limited to 4,096 characters. Messages exceeding this limit are rejected with an error response.
</Warning>

### Interactive Chat Loop

```python
import asyncio
import websockets
from purecortex_sdk import PureCortexClient


async def interactive_chat() -> None:
    with PureCortexClient(api_key="ctx_your_key") as client:
        session = client.create_chat_session()
        uri = client.websocket_url(session["session_token"])

    async with websockets.connect(uri) as ws:
        print("Connected to PURECORTEX. Type 'quit' to exit.\n")
        while True:
            message = input("You: ")
            if message.lower() == "quit":
                break
            await ws.send(message)
            print(f"Agent: {await ws.recv()}\n")


asyncio.run(interactive_chat())
```

---

## Algorand On-Chain Reads

Use `algosdk` to query the AgentFactory contract state and agent token data directly from the Algorand indexer.

### Query AgentFactory State

```python
from algosdk.v2client import indexer

FACTORY_APP_ID = 757172168
CORTEX_ASSET_ID = 757172171
INDEXER_URL = "https://testnet-idx.algonode.cloud"

def get_factory_info():
    client = indexer.IndexerClient("", INDEXER_URL)

    # Get application info
    app_info = client.applications(FACTORY_APP_ID)
    app = app_info["application"]

    print(f"App ID: {app['id']}")
    print(f"Creator: {app['params']['creator']}")

    # Read global state
    for state in app["params"].get("global-state", []):
        key = state["key"]
        value = state["value"]
        print(f"  {key}: {value}")

    return app_info
```

### Query CORTEX Asset Info

```python
def get_cortex_info():
    client = indexer.IndexerClient("", INDEXER_URL)

    asset_info = client.asset_info(CORTEX_ASSET_ID)
    asset = asset_info["asset"]
    params = asset["params"]

    print(f"Asset ID:   {asset['index']}")
    print(f"Name:       {params['name']}")
    print(f"Unit:       {params['unit-name']}")
    print(f"Total:      {params['total']:,}")
    print(f"Decimals:   {params['decimals']}")
    print(f"URL:        {params.get('url', 'N/A')}")

    return asset_info
```

### Search for Agent Tokens

```python
def find_agent_tokens():
    """Find all ASAs created by the AgentFactory contract."""
    client = indexer.IndexerClient("", INDEXER_URL)

    # Get the factory's Algorand address
    app_info = client.applications(FACTORY_APP_ID)
    # The factory creates assets as inner transactions
    # Search for assets where the creator is the application address

    txns = client.search_transactions(
        application_id=FACTORY_APP_ID,
        txn_type="acfg",  # Asset config transactions
    )

    agents = []
    for txn in txns.get("transactions", []):
        if "created-asset-index" in txn.get("asset-config-transaction", {}):
            asset_id = txn["asset-config-transaction"]["created-asset-index"]
            agents.append(asset_id)
            print(f"Agent token found: ASA {asset_id}")

    return agents
```

---

## Full Example

```python
import asyncio
import httpx

BASE_URL = "https://purecortex.ai"

async def main():
    async with httpx.AsyncClient() as client:
        # 1. Get supply info
        supply = (await client.get(f"{BASE_URL}/api/transparency/supply")).json()
        print(f"CORTEX Total Supply: {supply['total_supply']:,}\n")

        # 2. List agents
        registry = (await client.get(f"{BASE_URL}/api/agents/registry")).json()
        for agent in registry["agents"]:
            print(f"Agent: {agent['name']} ({agent['status']})")

        # 3. Chat with the Senator
        chat = (await client.post(
            f"{BASE_URL}/api/agents/senator/chat",
            headers={"X-API-Key": os.environ["PURECORTEX_API_KEY"]},
            json={"message": "Summarize the governance framework."},
        )).json()
        print(f"\nSenator says: {chat['response']}")

asyncio.run(main())
```
